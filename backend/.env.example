GEMINI_API_KEY="YOUR_API_KEY"
AWS_BACKEND_ACCOUNT_ID="YOUR_AWS_BACKEND_ACCOUNT_ID"
WEBHOOK_DOMAIN="YOUR_NGROK_WEBHOOK_DOMAIN"

# Auth & encrypted storage (required for persistent Cloud Assistant)
APP_SECRET_KEY="min-16-chars-random-string-for-session-signing"
APP_ENCRYPTION_KEY="paste-output-of-command-below (run in the root terminal)"
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# Optional: SQLite path (default sqlite:///./app.db under cwd when running uvicorn)
# DATABASE_URL="sqlite:///./app.db"
